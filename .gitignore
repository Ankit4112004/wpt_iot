*.pdf
.DS_Store
__pycache__
*.pyc
node_modules
.env

# ML artifacts — regenerated by ml/train.py (run at build time on Render)
*.pkl

# Raw datasets — fetched by ml/get_data.py, not committed (large)
ml/data/raw/*.csv

# Python venvs / caches
.venv
venv
*.egg-info

# Local SQLite dev database + env secrets
*.db
.env
