# Copy to .env for production builds. In local dev you can leave this unset —
# Vite proxies /api to the backend on localhost:8000 (see vite.config.js).

# Deployed backend URL (e.g. your Render service):
# VITE_API_BASE=https://ev-wpt-backend.onrender.com
VITE_API_BASE=
